# Obsidian Plugin Development Prompt: Fretboard SVG Renderer

## 1. Overview & Philosophy
ObsidianのMarkdownコードブロック内で、YAMLベースの軽量な記法を用いてギターの指板図（Fretboard）をSVGとして動的にレンダリングするプラグインを作成してください。
**Philosophy:** このプラグインの目標は「コード進行の分析から、ペンタトニックやモード・スケールのポジション確認まで、ギター学習に必要なあらゆる図解を **"最小のタイピング"** と **"最高の見栄え"** で実現する最強のツール」にすることです。
目指すビジュアルは無駄のないクリーンなデザインです（指定フレットの太いナット線、丸や四角の図形、度数や音名の表示を備えたもの）。

## 2. Configuration Layers (System / Global / Local)
設定は3段階のレイヤーで構成され、**後段ほど優先度が高くなります（Local > Global > System）**。同じキーが複数レイヤーで指定された場合、より優先度の高いレイヤーの値で上書きしてください。

| レイヤー | 定義場所 | スコープ | 優先度 |
| :--- | :--- | :--- | :--- |
| **System** | Obsidianのプラグイン設定画面 (Settings UI) | インストール単位（Vault内の全```fretboardブロック） | 低（フォールバック） |
| **Global** | Vaultルートの `fretboard-renderer.yaml` | そのVault内の全```fretboardブロック | 中 |
| **Local** | 各```fretboardコードブロック内のYAML | そのブロック1つのみ | 高（最終上書き） |

### 2.1 System Settings（プラグインのデフォルト設定）
Obsidianのプラグイン設定画面から、以下の項目をデフォルト値として設定可能にしてください。Global/Localで指定されなかった場合の最終フォールバックです。

#### [Layout & Dimensions]
*   **Orientation:** `horizontal` (デフォルト) / `vertical`
*   **Strings:** デフォルト `6`
*   **Fret Count:** デフォルト `4` (描画するフレット幅)
*   **String Spacing & Fret Spacing:** SVG描画時のピクセル幅
*   **Size:** デフォルト `1`。`String Spacing`・`Fret Spacing`・`Note Size`・`Label Font Size`（いずれもSystem/Global値）全体に一括で掛かる倍率。個々の図ごとに調整するのではなく、Vault全体の見た目を一括で縮小・拡大したい場合に使う（例: `0.7`で全体を70%サイズに）。3.2節のLocal限定`size`（個々の図だけの倍率）とは独立した別レイヤーで、両者は掛け算で合成される（`実効倍率 = System/Globalのsize × その図のLocal size`）——どちらかがもう一方を無効化して上書きすることはない。

#### [Display & Style]
*   **Label Mode:** `interval` (度数/デフォルト) / `note` (音名) / `none`。`note`は絶対モード（4.1節: `startFret`指定、または明示的な開放弦を含む）の時だけ音名を自動表示する。相対/移動モードでは実際の音がポジションに依存し確定しないため、`note`モードでも自動ラベルは表示しない（度数は形が決まれば常に一意なので`interval`モードはこの制約を受けない）。
*   **Accidental:** `sharp` (#) / `flat` (b)
*   **Default Shape:** `circle` (〇/デフォルト) / `square` (□) / `triangle` (△) / `diamond` (◇) / `octagon` (八角形) / `doublecircle` (◎) / `x` (×字) / `none`（枠なし。3.1節の`shape`の`none`と同じ挙動。ラベルのみを見せたい場合の全体デフォルトとして使う）
*   **String Note Default Shape:** `circle` / `square` / `triangle` / `diamond` / `octagon` / `doublecircle` / `x` / `none`（枠なし/デフォルト）。`stringNotes`（3.2節参照）専用のデフォルト形状で、上記`Default Shape`（通常の`notes`用）とは独立。弦ごとの注記は「押弦されている実在の音」ではなくメタ情報（弦番号・指番号等）であることが多く、丸や四角で強調するより文字だけの方が自然なため、既定を`none`にする。`stringNotes`のラベル文字は、この設定の値に関わらず常に控えめな色（`--text-muted`相当）で描画する。
*   **Fill Style:** `filled` (黒塗り・文字白) / `outlined` (白抜き・文字黒/デフォルト)
*   **Nut Style:** `thick` (太線/デフォルト) / `double` (二重線)。ナット（0フレット位置）の線の描画スタイル。
*   **Fret Numbering:** `all` (全て表示) / `dotted` (ドットが配置されているフレットのみ表示) / `inlay` (指板インレイの位置のみ表示。3, 5, 7, 9, 12, 15, 17, 19, 21, 24, ... という標準的なギターのポジションマーク位置。音の有無に関わらず常にこの位置を表示する/デフォルト) / `none` (非表示)
*   **Naming Mode:** `chord` (コード名を自動生成/デフォルト) / `scale` (スケール名を自動生成)。詳細は4.4節参照。
*   **Chord Symbol Style:** `standard` (Pop標準/デフォルト) / `berklee` (理論重視) / `jazz` (Real Book形式)。コード名の自動生成時、サフィックスの表記ルールを切り替える。
    *   `standard`: 例 `Cmaj7`, `Cm7`, `Cm7(b5)`, `Cdim7`, `Caug`
    *   `berklee`: 例 `Cmaj7`, `C-7`, `C-7(b5)`, `Cdim7`, `C+`
    *   `jazz`: 例 `CΔ7`, `C-7`, `Cø7`, `C°7`, `C+`
*   **Omit Notation:** `on` / `off` (デフォルト: `off`)。コード名自動生成時、理論上期待される構成音が実際には含まれていないことを `(omit1)` / `(omit3)` / `(omit5)` として明示する機能。**デフォルトはオフ**にすること。ギターでは5度を省略した運指が非常に多く、常に表示すると大半のコードが `(omit5)` だらけになりノイズになるため。詳細は4.6節・4.7節参照。
*   **Show Inversions:** `on` / `off` (デフォルト: `off`)。コード名自動生成時、ベース音がそのコード自体の構成音（3度・5度・7度）と一致する「単なる転回形」の場合に、スラッシュ表記（分数コード表記）を表示するかどうか。**デフォルトはオフ**にすること。実際のコード譜では転回形をあえて明示しないパターンの方が多いため。ベース音がコードの構成音に含まれないテンション音等の場合（真の分数コード）は、この設定に関わらず常にスラッシュ表記を行う。詳細は4.6節1.参照。
*   **Root Notation:** `absolute` (実音名/デフォルト) / `degree` (度数名)。自動生成タイトルのルート音（および分数コードのベース音）を、実際の音名の代わりに`key`（3.2節参照）を基準としたローマ数字の度数表記（I, bII, II, ... VII）にするかどうか。`degree`を指定していても、その図に`key`が設定されていない場合、または図がRelative Mode（4.1節、ムーバブルな形で絶対ピッチが未確定）の場合は、常にAbsoluteと同じ挙動にフォールバックする。詳細は4.8節参照。

#### [Tuning & Fallback]
*   **Default Tuning:** `E,A,D,G,B,E` (カンマ区切り、低音弦から高音弦の順。絶対ピッチ基準)
*   **Omitted String Behavior:** `open` (f: 0 補完/デフォルト) / `muted` (f: x 補完) / `none` (何も描画しない)。記述が省略された弦のフォールバック動作。

#### [Note Appearance]
*   **Note Size (px):** 音のドット（形）の基準半径。デフォルト `9`。個々のノートの`sizeAdjust`（-5〜5）がこの値に加算される。
*   **Label Font Size (px):** 音のラベル文字の基準フォントサイズ。デフォルト `11`。個々のノートの`labelSizeAdjust`（-5〜5）がこの値に加算される。実際の描画フォントサイズには`4px`の下限（floor）があり、`labelSizeAdjust`で`4px`を下回る値になった場合は黙って`4px`に張り付く（縮小しすぎて文字が潰れるのを防ぐため）。

これらはユーザーがプラグイン本体のCSSファイルを直接編集することを想定していないため、見た目の基準値をSystem設定のpx値として持たせ、細かい調整はLocal（ノート単位）のAdjustオプションで行う設計とすること。

### 2.2 Global Settings（Vault共通のYAML設定ファイル）
Vaultのルートディレクトリに **`fretboard-renderer.yaml`** というファイルを置くことで、そのVault内の全```fretboardブロックに適用されるデフォルト値を、System設定より優先して上書きできます。

*   指定できるキーはSystem設定と全く同じ（2.1節の全項目: orientation, strings, fretCount, stringSpacing, fretSpacing, size, labelMode, accidental, defaultShape, stringNoteDefaultShape, fillStyle, nutStyle, fretNumbering, defaultTuning, omittedStringBehavior, noteSize, labelFontSize, namingMode, chordSymbolStyle, omitNotation, showInversions, rootNotation）。`key`はLocal限定のオプションのため、Global（およびSystem）には含まれない（3.2節・4.8節参照）。
*   ファイルが存在しない場合、またはキーが省略されている場合は、Systemの値がそのまま使われる。
*   ファイルの構文エラーや不正な値がある場合は、プラグインやVault内の描画をクラッシュさせず、Obsidianの通知（Notice）で一度だけエラー内容を表示した上で、System設定にフォールバックして動作を継続すること（個々のブロックにはエラーを出さない。原因がVault共通ファイル側にあるため）。
*   ファイルを編集して保存すると、次にレンダリングされるブロックから自動的に反映される（プラグインの再起動やリロードは不要）。

【記述例: Vaultルートの `fretboard-renderer.yaml`】
```yaml
orientation: vertical
fretCount: 5
labelMode: note
```

### 2.3 Local Settings（```fretboardブロックごとのYAML）
各ノート内の```fretboardコードブロックに直接書くYAMLです。System・Globalの値を最終的に上書きする、最も優先度の高い設定です。書式は3節を参照してください。

## 3. Local Settings Syntax (Per-Diagram YAML)
ユーザーは Markdown ノート内に、Obsidian標準の `parseYaml` で解析可能な形式で記述します。
**`notes` プロパティのみが必須（Mandatory）であり、他はすべて任意（Optional）です。**

### 【記述例】
`startFret: 5`により、`notes`/`boxes`/`paths`内のフレット番号（1〜4）は「ポジション1起点の相対値」として自動的に5〜8フレットへ変換される（3.2節参照）。この`notes`ブロックはそのまま、`startFret`の値を変えるだけで指板上の別の位置に移動できる。
```fretboard
title: Am Pentatonic (Box 1)
visible: 1-6
startFret: 5
frets: 4
boxes:
  - {frets: "1-4", style: dashed}
paths:
  - [[6,1], [6,4], [5,1], [5,3]]
notes:
  - {s: 6, f: 1, label: root, shape: square}
  - [6, 4] # 配列による省略記法も許容
  - {s: 5, f: 1}
  - {s: 5, f: 3, finger: 3, ghost: true}
```

### 3.1 必須項目 (Mandatory)
*   **`notes` (Array):** 音の配置データ。オブジェクト `{s: 6, f: 5, label: root}` または、タイピングを減らすための配列省略記法 `[6, 5, "root"]` (インデックス順: s, f, label, shape, finger) の両方を許容し、内部で正規化してください。
    *   `s` (必須): 弦番号 (1〜N)。
    *   `f` (必須): フレット番号。`0` (開放弦)、`1以上` (押弦)、`x` (ミュート)。
        *   `0` と `x` は**グリッド（フレット列）の一部ではない**。常にグリッド外側の専用ヘッダ領域に描画し、各列に印字されるフレット番号としても一切カウントしないこと。あくまで「YAML記述上、開放弦・ミュート弦を指定するための値」であり、実在するフレット位置ではない。
        *   `x` は cross (×) 記号を描画。
    *   `label` (任意): `root` で度数自動計算。任意の文字列（`maj7`, `9`, `11` 等のテンション表記を含む）で強制表示。度数の自動算出は1オクターブ内でしか判定できない（9thと2nd、11thと4th等を区別できない）ため、テンションを正しく表示したい場合はユーザーがこの`label`で明示すること。
    *   `shape` (任意): `circle`, `square`, `triangle`, `diamond`（◇）, `octagon`（八角形）, `doublecircle`（◎。外側の輪＋中心の小さな塗りドットという固定の二重丸表示。`x`と同じ理由で`Fill Style`の影響を受けず、常に「外輪＝outlined相当、中心ドット＝filled相当」の配色で描画する——`Fill Style`によって塗り分けを変えてしまうと、この形状の存在意義である二層のコントラストが消えてしまうため）, `x`（×字。3.1節冒頭の`f: x`によるミュート弦の×マーク——常にヘッダーレーンに描画される——とは別物で、こちらは実際に押弦された任意の位置に使える形状の1つ）, `none`。`none`は枠線を一切描画せず、ラベル文字だけが見える表示にする。ただし文字が背後のフレット線・弦線と交差すると読みにくくなるため、`none`でも実際には常に枠なし（stroke無し）の円を背景として描画し、その上にラベルを重ねる。ラベルが無い音（`label`未指定）の場合も同様にこの枠なしの円のみが描画され、完全に非表示にはならない（グリッド上に音の位置が存在すること自体は常に視認できる必要があるため）。`Fill Style`（3.1節の`fillStyle`、またはSystem/Globalの既定値）は`none`でも通常通り適用され、円の塗り色（`filled`なら濃色、`outlined`なら背景色）を決めるが、`outlined`の場合に通常表示される縁取り線（枠線）だけが常に省略される。`x`と`doublecircle`は`Fill Style`の影響を受けない固定表示の形状である（`x`は塗る内側の面が無いため、`doublecircle`は二層配色自体が形状の定義そのものであるため）。
    *   `finger` (任意): 運指番号（1, 2, 3, 4など）。ドットの外側（下または上）に小さく印字。
    *   `ghost` (任意): Boolean。trueの場合、枠線を点線(dashed)または半透明で描画。
    *   `virtual` (任意): Boolean。trueの場合、この音を実際に押弦・発音される音としては扱わず、コード名の自動算出（Root判定・度数計算・テンション計算）の基準点としてのみ機能させる。図形（丸・四角等）は一切描画せず、その位置にラベル文字だけを丸括弧付きで表示する（例: Rootなら`(R)`、5度なら`(5)`）。ラベルが空の場合は何も描画しない。分数コードのベース音判定（4.6節1.）では常に除外される（＝物理的に鳴っていない音として扱う）。詳細は4.7節参照。
    *   `class` (任意): 任意のCSSクラス名（ハイライト用）。
    *   `color` (任意): この音1つだけの色をCSS色文字列（`red`, `#ff0000`等）で指定。指定時はSystem/Globalの`Fill Style`やRoot強調色より優先される。省略時はデフォルト（Rootは`--interactive-accent`、それ以外は`--text-normal`）。
    *   `fillStyle` (任意): `filled` / `outlined`。この音1つだけSystem/Globalの`Fill Style`を上書きする。
    *   `sizeAdjust` (任意, 整数 -5〜5): この音1つだけの形の大きさを、System/Globalの`Note Size`からピクセル単位で微調整する。
    *   `labelSizeAdjust` (任意, 整数 -5〜5): この音1つだけのラベル文字サイズを、System/Globalの`Label Font Size`からピクセル単位で微調整する。
    *   `opacity` (任意, 数値 0〜1): この音の形＋ラベルをまとめて薄く表示する（例: 経過音・アプローチノート・任意で省略可能な音などを、視覚的に「弱い」情報として示す用途）。形とラベルはSVGの`<g>`要素でグループ化してから不透明度を適用するため、両者が一体として同じ濃さで薄くなる（ラベル文字だけが形よりさらに薄れて潰れる、といったことが起きない）。`ghost: true`（常に固定の不透明度＋点線）とは独立した仕組みで、両方指定した場合は互いを上書きせず重ね合わさる（掛け算）。
    *   `color`, `fillStyle`, `sizeAdjust`, `labelSizeAdjust`, `opacity` はオブジェクト形式でのみ指定可能（配列の省略記法には含めない。位置引数が増えすぎて可読性を損なうため）。

### 3.2 任意項目 (Optional)
*   **`title` (String | Boolean):** グラフ上部に表示。省略時は算出された度数からコード名を自動生成（4.1節参照）。自動生成されたコード名がテンション表記（9th/11th/13th等）を正しく表せない場合は、ここで明示的に上書きできる（例: `title: Cmaj9`）。`title: false`を指定すると、コード名の自動算出自体は内部的に行われたまま（度数計算・他機能からの参照には影響しない）、この図の上には一切表示しない。
*   **`startFret` (Number):** 描画領域の左端となるフレット番号。有無で描画モードが切り替わる（4.1節参照）。**`startFret`を明示指定した場合、`notes`/`boxes`/`paths`/`barre`内のフレット番号は「1番目のポジションからの相対値」として解釈され、`absolute = f + max(startFret, 1) - 1`で実際のフレットに変換される**（`0`と`x`は常にこの変換の対象外で、常にそのまま開放弦・ミュートとして扱う）。これにより、同じ`notes`ブロックを`startFret`の値だけ変えて使い回せる（例: ペンタトニックのボックスを指板上のどこにでも移動できる）。`startFret`が`0`または`1`のときはオフセットが`0`になるため、実質的に変換は起きず、絶対的なフレット番号をそのまま書く従来通りの挙動になる。
*   **`frets` (Number):** 描画するフレット幅。省略時はSystem/Globalの`Fret Count`が使われるが、それだけでは押弦している最も高いフレットの音がグリッドからはみ出す場合は、その音が収まる幅まで自動的に拡張すること（弦の本数を`notes`の最大値に合わせて自動拡張するのと同じ考え方）。`frets`を明示指定した場合はユーザーの意図を優先し、自動拡張しない。
*   **`orientation` (String):** `horizontal` / `vertical`。System/Globalの向きをこの図だけ上書きする。
*   **`namingMode` (String):** `chord` / `scale`。System/Globalの命名モードをこの図だけ上書きする（4.4節参照）。
*   **`chordSymbolStyle` (String):** `standard` / `berklee` / `jazz`。System/Globalのコード表記スタイルをこの図の自動生成タイトルだけ上書きする（2.1節参照）。
*   **`omitNotation` (Boolean):** System/GlobalのOmit Notationをこの図の自動生成タイトルだけ上書きする（4.6節・4.7節参照）。
*   **`showInversions` (Boolean):** System/GlobalのShow Inversionsをこの図の自動生成タイトルだけ上書きする（4.6節1.参照）。
*   **`rootNotation` (String):** `absolute` / `degree`。System/GlobalのRoot Notationをこの図の自動生成タイトルだけ上書きする（4.8節参照）。
*   **`key` (String):** この図のルート音を分析する基準となるトニック音名（例: `"C"`, `"F#"`, `"Bb"`）。System/Global設定は無く、この図だけのLocal限定オプション（曲によってキーが異なる/転調するのが普通のため）。`rootNotation: degree`を機能させるために必要な値であり、また指定するだけで図の左上に`Key: C`のようなラベルを表示する（`rootNotation`の値に関わらず常に表示される）。詳細は4.8節参照。
*   **`scaleAnalyze` (Boolean):** `namingMode`が`scale`のときのみ意味を持つ。System/Global設定は無く、この図だけのLocal限定オプション（4.4節参照）。
*   **`omittedStringBehavior` (String):** `open` / `muted` / `none`。System/Globalの`Omitted String Behavior`（2.1節）をこの図だけ上書きする。`visible`で表示範囲を絞った図で、明示的に`notes`を書いていない弦（＝自動補完される弦）の見た目だけをその図限定で変えたい場合に使う（例: `visible: "4-6"`かつ弦4・6には何も弾かせたくない図で、System全体の`open`設定は変えずにこの図だけ`none`にする）。特定の1本の弦だけ挙動を変えたい場合は、この設定ではなくその弦を`notes`に明示的に書き（例: `{s: 4, f: 0, shape: none}`）、`shape`等で個別に制御すること——このオプションは「`notes`に一切書かれていない弦全体」に対するフォールバックのみを変更する。
*   **`size` (Number):** この図だけの表示倍率。System/Globalの `stringSpacing`/`fretSpacing`（`fretSpacingAdjust`/`stringSpacingAdjust`適用後の値）・`noteSize`・`labelFontSize`に乗算される（例: `0.6` で60%サイズ）。複数の図を小さく並べたい場合に使う。System/Globalの`Size`（2.1節参照、Vault全体の倍率）とは独立したレイヤーで、両者は掛け算で合成される（片方がもう片方を上書きすることはない）。
*   **`fretSpacingAdjust` / `stringSpacingAdjust` (Number, 整数 -5〜5):** System/Globalの `fretSpacing`/`stringSpacing`（ピクセル）に対する微調整量。ユーザーはプラグインのCSSファイルを直接編集する想定ではないため、ピクセル単位の細かい見た目調整はこのYAMLオプションで行う。`size`より先に加算され、その後`size`が乗算される。
*   **`visible` (String):** 描画する弦の範囲。例: `"1-4"`。指定された場合、SVGのY軸（縦幅）の計算を動的に変更し、不要な弦は描画しないこと。
*   **`barre` (Array):** `{fret, start, end}`。セーハ記号を描画。
*   **`boxes` (Array):** スケールのポジション等を枠線で囲む。各要素は次の2形式のいずれかで書ける（両者は排他。どちらか一方のみ指定可能）:
    *   **矩形指定（従来通り）:** `{frets: "5-8", strings: "1-6", style: "dashed"}`。`frets`必須、`strings`省略時は表示中の全弦。
    *   **多角形指定:** `{points: [[6,5], [3,5], [3,8], [6,8]], style: "dashed"}`。3点以上の`[string, fret]`座標を頂点として結んだ任意の多角形（三角形〜複雑な形まで）を描画する。座標系は`paths`の`points`と全く同じ（3.2節`paths`参照）で、`notes`と同じ実フレット位置基準だが小数も許容する（例: `4.5`はフレット4と5の境界線）。この小数許容により、多角形指定は矩形指定の完全な上位互換になる——境界に半整数座標を置けば、従来の矩形`{frets: "1-4", strings: "2-5"}`と全く同じ形を`points`でも再現できる。
    *   `style` (任意): `solid`（実線/デフォルト） / `dashed`（点線）。矩形・多角形どちらの指定方法でも共通。
    *   `color` (任意): この枠1つだけの線の色をCSS色文字列で上書きする（ノートの`color`と同じ規約）。`fill: true`と併用時は塗りの色にもなる。
    *   `fill` (任意 Boolean, デフォルト`false`): `true`にすると内部を固定の低い不透明度（半透明のハイライトとして見える程度。パラメータ化はしない）で塗りつぶす。`color`未指定時はデフォルトのmuted色で塗る。
    *   **角の丸め:** 矩形・多角形のいずれも、描画時は常にコーナーをグリッド間隔に応じたわずかな量で丸める（鋭角にはしない）。凹んだ角（L字型など）も同様に丸まる。これは常時適用される描画上の仕様でありパラメータ化はしない（従来の矩形指定の見た目も、このコーナー丸めにより鋭角ではなくなる）。
*   **`paths` (Array):** ドット同士を線で結ぶ。各要素は次の2形式のいずれかで書ける:
    *   **省略記法（従来通り）:** `[[6,5], [6,8], [5,5]]` のように`[string, fret]`座標の配列をそのまま渡す。`style: solid`（実線、デフォルト）・色指定なしとして扱われる。
    *   **オブジェクト形式:** `{points: [[6,5], [6,8]], style: dashed, color: "red"}`。座標配列を`points`に渡し、`style`・`color`を個別に指定できる。
    *   `points` (必須, オブジェクト形式のみ): 座標（`[string, fret]`）の配列。線で結ぶ順序通りに並べる。
    *   `style` (任意): `solid`（実線/デフォルト） / `dashed`（点線） / `thick`（バレー記号と同じ太さ・線幅・不透明度（オパシティ）の実線。斜めのバレーを表現したい場合や、複数のドットのまとまりを強調したい場合に使う）。
    *   `color` (任意): この線1本だけの色をCSS色文字列で指定する（ノートの`color`と同じ規約）。省略時はデフォルト色（`--text-muted`）。矢印（`arrow`）を使う場合、矢じり（マーカー）の色にもこの`color`が反映される。
    *   `arrow` (任意): `none`（矢じりなし/デフォルト） / `single`（`points`の最後の点にだけ矢じりを描く） / `double`（両端に矢じりを描く）。向きを表す独立したフィールドは無く、`single`の矢の向きは常に「`points`に書いた順序の、後ろの点」を指す——逆向きにしたい場合は`points`を逆順に書く。
    *   `curve` (任意, Boolean, デフォルト`false`): `true`にすると、点同士を直線ではなく滑らかな曲線で結ぶ。点が2つだけの場合は直線に丸みを一定量つけた弓なりの曲線、3点以上の場合は全ての点を通る滑らかなスプライン曲線になる（近似ではなく、指定した点を必ず通る）。
    *   複数の`paths`をそれぞれ異なる`style`・`color`・`arrow`・`curve`で描画し、グループを視覚的に描き分けることができる（例: あるフレーズの経過的な動きは曲線の片矢印、コードの主要な骨格はバレー同等の太さの実線で強調、など）。
*   **`stringNotes` (Array):** 特定のフレット位置ではなく**弦そのもの**に紐づく注記を、グリッドの外側に描画する。オブジェクト形式のみ（`notes`のような配列省略記法は無い。使用頻度が低く簡潔さの恩恵が小さいため、`barre`/`boxes`と同じ判断）。
    *   `s` (必須): 弦番号 (1〜N)。ピッチ・フレットとは無関係の、純粋に「その弦の行」への配置。
    *   `label` (任意): 表示する文字列。ピッチに紐づかないため`root`等の自動計算は行わない（現状の主用途は指番号、例: `label: "4"`）。
    *   `side` (任意): `trailing`（デフォルト） / `leading`。どちら側のグリッド外側に描画するかを指定する。`trailing`（従来通り）はHorizontal Orientationでグリッドの**右側**、Vertical Orientationで**下側**——開放弦/ミュート弦（`f: 0`/`f: x`）のヘッダーレーンとちょうど反対側の位置。`leading`はその逆で、Horizontal Orientationでグリッドの**左側**（ヘッダーレーンよりさらに外側）、Vertical Orientationで**上側**。`visible`で表示弦を絞り込んだ図（例: `visible: "4-6"`）では、グリッド上の行がどの弦番号に対応するか見た目だけでは分からなくなるため、`leading`側に実際の弦番号を`label`として表示する、といった用途を想定する。
    *   `shape` / `ghost` / `class` / `color` / `fillStyle` / `sizeAdjust` / `labelSizeAdjust` (すべて任意): `notes`の同名フィールドと全く同じ意味を持ち、`notes`と同じ描画ロジック（形＋ラベル）をそのまま再利用して見た目の一貫性を保つ。デフォルト値はSystem/Globalの`Note Size`、`Label Font Size`を`notes`と共有するが、**`shape`のデフォルトだけは`notes`用の`Default Shape`ではなく専用の`String Note Default Shape`（2.1節参照、既定`none`）を使う**——弦ごとの注記は実在の押弦音ではなくメタ情報であることが多いため。またラベル文字は、常に控えめな色で描画される（`notes`のラベルとは異なる、`stringNotes`固有の描画仕様）。
        *   **`shape: none`の挙動は`notes`と異なる:** `notes`の`shape: none`は3.1節の通り「常に枠なしの円を背景として描画し、その上にラベルを重ねる」——押弦位置は実在のピッチであり、グリッド上での存在自体を常に視認できる必要があるため。`stringNotes`にはこの制約が無い（実在のピッチではなく補足情報）ため、`shape: none`（既定）の場合は`fillStyle`の値に関わらず背景の円を一切描画せず、純粋にラベル文字だけを表示する。`fillStyle`が意味を持つのは、`shape`を`circle`/`square`/`triangle`に明示的に上書きした場合のみ。`f`・`finger`・`virtual`に相当するフィールドは無い（ピッチが存在しないため`virtual`は無意味、`finger`はこの機能の`label`自体がその役割を果たすため不要）。
    *   `visible`で指定した範囲外の弦の`stringNotes`は、通常の`notes`と同様に描画から除外される。また`notes`の場合と同様、`stringNotes`の`s`が既存の弦数を超える場合はグリッドの弦数を自動拡張する。
    *   **将来の自動指番号機能に向けた布石:** 現状は手動での指番号付与が主用途だが、将来的にコードフォームから指番号を自動算出する機能を検討しており、`stringNotes`はその出力先となることを想定した設計である。

### 3.3 複数の図を横に並べる (`diagrams`)
別々の```fretboardブロックを連続して書いても、それらが実際に横並びになるかはObsidian側のMarkdownレンダリング構造（Reading View / Live Previewでのブロックのラップ方法）に依存し、プラグイン側から確実に制御できない（CSSスニペットで`.fretboard-block { display: inline-block }`等を当てても効かないケースがある）。これを回避するため、**1つの```fretboardブロックの中に複数の図を並べて描画する`diagrams`という書き方をサポートする**こと。

```fretboard
diagrams:
  - title: Cmaj7
    startFret: 0
    size: 0.6
    notes:
      - {s: 5, f: 3, label: root}
      - [4, 2]
      - [3, 0]
      - [2, 0]
  - title: Dm7
    startFret: 0
    size: 0.6
    notes:
      - {s: 4, f: 0, label: root}
      - [3, 2]
      - [2, 1]
      - [1, 1]
```

*   トップレベルのキーが`notes`ではなく`diagrams`（図の配列）である場合、この「複数図モード」として扱う。`diagrams`と`notes`など他のトップレベルキーを同時に指定するのはエラーとする。
*   `diagrams`の各要素は、通常の単一の図とまったく同じスキーマ（3.1, 3.2節の全項目）を持つ。
*   複数図モードでは、1つのコードブロックのコンテナ要素の中に、プラグインが自前でflexboxのラッパー（例: `display:flex; flex-wrap:wrap;`）を生成し、その中に各図のSVGを並べて描画すること。これにより、Obsidianがコードブロックをどうラップするかに関わらず、確実に横並び表示できる（レイアウトが完全にプラグインの管理下にあるDOM内で完結するため）。
*   エラー処理は3.1/3.2と同様、`diagrams[0].notes`のように該当する図のインデックスを含めたエラーメッセージにすること。
*   上記の配列形式は、実は**`diagrams: {fretboards: [配列]}`というオブジェクト形式の省略記法**である（3.4節の`progression`付きオブジェクト形式とは`progression`の有無で区別される）。`progression`を書かない場合、このオブジェクト形式は配列形式と全く同じ図を並べるが、加えて**`size`（任意）**を同じ階層に書くことで、`fretboards`内の全ての図に共通のデフォルト`size`を一括指定できる（個々の図が自前で`size`を指定している場合はそちらを優先し、上書きしない）。1つ1つの図に同じ`size`を書き続ける手間を省くためのショートカット。
    ```fretboard
    diagrams:
      size: 0.6
      fretboards:
        - {title: Cmaj7, startFret: 0, notes: [...]}
        - {title: Dm7, startFret: 0, notes: [...]}
    ```

### 3.4 コード進行シート (`diagrams`のオブジェクト形式、実験的機能)

**この機能は実験的であり、将来的に仕様が変わる可能性がある。** 五線譜そのものの表現は目的としない（それは`abcjs`等の外部ツールの役割と位置づけ、本プラグインは指板図を部品として提供することに徹する）。目的は、市販の教則本によくある「コード進行の1行＋その下に対応する指板図」という形を、YAMLだけで組み立てられるようにすること。

`diagrams`（3.3節）を、配列に加えて**オブジェクト形式**でも書けるように拡張する。配列形式は`{fretboards: <配列>}`の省略記法として扱われ、3.3節の既存の挙動（複数の図の単純な横並び）とは完全互換で変わらない。

```fretboard
diagrams:
  progression: [[Dm7(b6)], [G7(alt)], [Cm7], [F7]]
  variations:
    - fretboards: # ①
        - {startFret: 4, notes: [...]}
        - {startFret: 4, notes: [...]}
        - {startFret: 4, notes: [...]}
        - {startFret: 4, notes: [...]}
    - fretboards: # ②
        - {startFret: 9, notes: [...]}
        - {startFret: 9, notes: [...]}
        - {startFret: 9, notes: [...]}
        - {startFret: 9, notes: [...]}
```

#### `progression`（コード進行の定義）

「小節」の配列。**各小節は、1コードしか無い場合も含めて必ずその小節専用の配列として書く**（例: `[Cm7]`）。1小節内でコードが変わる場合は、その配列に複数の「スロット」を並べる（例: `[Dm7(b6), G7(alt)]`で1小節に2コード）。

この「角括弧1つ＝必ず小節1つ」というルールに例外は無い。`progression: [Cm7, Dm7]`のような、小節を囲む角括弧を省いたフラットな書き方は**エラーとする**（意図的にサポートしない）——このフラット表記は「Cm7とDm7、それぞれ1小節ずつの2小節」なのか「1小節の中でCm7からDm7へコードチェンジする」なのかが見た目だけでは判別できず、書く側・読む側どちらにとっても紛らわしいための廃止。正しくは前者を`[[Cm7], [Dm7]]`、後者を`[[Cm7, Dm7]]`と書く。

スロットの値は3種類:

*   **明示文字列**（例: `"Dm7"`）: 強制表示。4.5節のタイポグラフィ解析は行わずプレーンテキストとして描画する（ユーザー指定の`title`と同じ規約）。
*   **`_N`（`_`+数値）**: 自動導出プレースホルダー。`N`はこの`progression`全体を通した1始まりの通し番号でなければならず、実際の位置とズレている場合はパースエラーとする（5節の「暗黙に無視しない」方針）。表示テキストは、下記`fretboards`側の対応するセルの計算済みタイトルから自動的に流用する。
*   **`%`**: 直前のスロットの解決済み表示結果（文字列とタイポグラフィ分類の両方）をそのまま複製する。先頭スロットに`%`を書くことはエラー（複製元が存在しない）。

`progression`自体には改行・セグメントの区切りマーカーは存在しない。小節数が多く1行に収まらない場合の折り返しは、3.3節と同じくレンダラー側のflexbox wrapに任せる（データ構造上の概念ではない）。

#### `fretboards` / `variations`（対応する指板図）

*   **`fretboards`（単一パス、`variations`と排他）:** `progression`の小節と同じ添字で1:1対応する配列。`fretboards[i]`の要素数は`progression[i]`のスロット数と厳密に一致しなければならない（不一致はパースエラー）。各要素＝「セル」。セルは素のオブジェクト（3.1/3.2節と同一スキーマの1つのダイアグラム設定）、または配列（同一スロットに複数のダイアグラムを並べる。コード自体は変えず、声部・トップノート違いなどのバリエーションを同じ小節内で見せたい場合に使う）のどちらかで書ける。
*   **`variations`（複数パス、`fretboards`と排他）:** `[{fretboards: [...]}, ...]`。各要素の`fretboards`は上記と全く同じルールに従う。教則本にあるような、同じ進行を異なるポジション/フォームで弾いた複数行（①②③④...）を、共通の`progression`ヘッダーの下に縦に積んで描画する。
*   **`size`（任意）:** このシート内の全てのセルのダイアグラムに適用される、シート全体共通の`size`（3.2節の`size`と同じ意味）のデフォルト値。個々のダイアグラムが自前で`size`を指定していない場合にのみ適用される（指定済みのダイアグラムには上書きせず、そのダイアグラム自身の値を優先する——両者を掛け合わせることはしない）。`fretboards`/`variations`のどちらのモードでも使える。1つ1つのセルに同じ`size`を書き続ける手間を省くためのショートカットで、System/Globalの`Size`（2.1節）とは独立して掛け算で合成される（`実効倍率 = System/Globalのsize × (このダイアグラム自身のsize ?? このシートのsize ?? 1)`）。

#### `title`との連携

`progression`が存在すること自体は、個々のダイアグラムの`title`表示を左右しない（3.2節の`title`は常に独立して従来通り動作する）。`progression`の自動導出プレースホルダー（`_N`）とダイアグラム側の`title`を明示的に結びつけたい場合のみ、`title`に以下の値を使う:

*   **`title: false`:** （3.2節参照）この図のtitleを表示しない。
*   **`title: $N`:** この図を`_N`（`progression`側のスロット番号`N`）の供給元として明示的に指名し、かつこの図自体には表示しない（`false`と同様の非表示効果を含む）。指名が無い`_N`は、デフォルトで最初のパス（`fretboards`、または`variations[0].fretboards`）の対応するセル内の最初のダイアグラムを供給元として扱う。同じ`N`に対して`$N`を持つダイアグラムが複数存在する場合はエラー（供給元が一意に決まらないため）。対応する`_N`が`progression`側に存在しない場合、`$N`は単に「この図のtitleを表示しない」という効果だけを持つ（转送先が無いだけで、エラーにはしない）。

## 4. Core Logic

### 4.1 描画モードの自動切り替え
*   **Absolute Mode (絶対座標):** 次のいずれかに該当する場合。
    1.  `startFret` が指定されている場合。
    2.  `startFret` が省略されていても、`notes` の中に明示的な開放弦（`f: 0`）が1つでも含まれる場合。開放弦を含む形は物理的に移動できない（ムーバブルではない）ため、絶対位置が確定しているとみなす。
    このモードでは、そこ（`startFret`、または開放弦を含む場合は0）を左端として描画し、絶対的なコード名（Cmaj7等）を生成する。ナット（0フレット位置）は、実際に左端が0の場合のみ太線/二重線で描画する。
*   **Relative Mode (相対座標/移動ド):** `startFret` が省略され、かつ`notes`内に開放弦が一切含まれない場合のみ。`notes` 内の `f` の最小値（0とxは除く）を自動的に左端フレットとし、各列のフレット番号を一切印字しない（絶対フレット位置が不確定なムーバブルな形のため）。相対コード名（□maj7等）を生成する。

### 4.2 Interval Auto-Calculation & Root Highlighting
`label: root` が指定された場合、Tuningの絶対ピッチ（MIDIノート番号）を基準に各弦の半音差分(Delta)を計算し、`%12`のモジュロ演算で度数 [1, b2, 2, m3, 3, 4, b5, 5, m6, 6, m7, M7] を割り当ててください。
また、**算出結果が `1` (Rootのオクターブ違い) となったすべてのノートについて、自動的に特別なスタイル（例: defaultShapeが円なら四角形にする、または色を変える）を適用**し、視覚的にRootが際立つようにしてください。

この自動算出は1オクターブ内の度数（1〜M7の12種類）しか区別できない仕様上の制約がある（例: 9thと2nd、11thと4thは区別できない）。テンション込みの正確な表記が必要な場合は、個々のノートの`label`、または図全体の`title`をユーザーが明示的に指定することで上書きする（3.1, 3.2節参照）。

**表示ラベルの調整（内部の度数管理とは独立）:** `Label Mode: interval` で実際にノートのドット内に印字する文字列については、以下の2点を内部の度数ラベル（`1`, `6`等。コード名生成やスケール判定で引き続き使われる）とは別に調整すること。
*   **Root (度数`1`) は `R` と表示する。** 運指番号（finger、1〜4）と数字が紛らわしいため。度数の内部表現・コード名生成・スケール判定には影響させず、あくまでノートのドット内に印字する文字列だけをこのルールで上書きする。
*   **ディミニッシュとして認識されたコード内の度数`6`（9半音、ディミニッシュ7thの`bb7`と異名同音）は `bb7` と表示する。** 判定条件はコード名生成でdim/dim7と判定する条件（m3 + b5 が構成音に含まれ、他の7th(m7/M7)を含まない）と同一にすること。この条件を満たさない場合（例: 通常のm6/6コード）は、度数`6`は従来通り`6`と表示する。

### 4.3 Default Behaviors (フォールバック)
`label: root` が一つも存在しない場合は度数の自動算出は行わず、label未指定の音は文字無しの「図形のみ（例: 黒塗り丸 ●）」として描画してください。省略された弦は `Omitted String Behavior` に従って補完してください。

### 4.4 Naming Mode（コード名 / スケール名の自動生成切り替え）
同じ音の集合でも、「コード（アルペジオ）として見た名前」と「スケールとして見た名前」は異なる（例: ルート・m3・4・5・m7の5音は、コードとしては`m7add11`のように見えるが、スケールとしてはマイナーペンタトニックである）。`Naming Mode`はこの解釈を切り替える設定。

*   **`chord` (デフォルト):** 従来通り、4.2節の度数集合からコード名サフィックス（`inferChordSuffix`相当のロジック）を生成する。
*   **`scale`:** フレーズ分析やソロのコピー用途を想定し、算出した度数の集合（`presentDegrees`）に最も近いスケールを辞書から推論（Best-match inference）し、スケール外の音は「経過音（Passing Notes）」として扱う。`chord`モードのような「完全一致しなければコード名にフォールバック」は行わず、常にベストフィットのスケール名を返す。
    *   **推論アルゴリズム (Scoring):** 
        `SCALES` 定義表の各スケールに対してスコアを計算する。
        1. スケール自身の構成音のうち、入力（`presentDegrees`）に含まれている度数1つにつき +1点（一致）
        2. スケール自身の構成音のうち、入力に含まれていない度数1つにつき -1点（欠落・ペナルティ）
        3. 入力に含まれているが、そのスケールの構成音ではない度数（経過音候補）1つにつき -1点（ペナルティ）
        スコアが最も高いスケールを「土台となるスケール（Base Scale）」として採用する。同点の場合は、より音数の少ない（シンプルな）スケールを優先する。それでも同点の場合は`SCALES`配列内での出現順（一般的なスケールほど手前に置く設計 — 後述）を最終的なタイブレークとする。

        3.の経過音ペナルティは、以下の理由で必須（単なる1./2.だけでは不十分）: 経過音を無罰にすると、「1音だけ経過音として残す小さいスケール」と「その音も含めて過不足なく説明できるやや大きいスケール」が同点になり得て、その場合タイブレーク（音数の少なさ優先）で常に前者が勝ってしまう。しかし後者（余さず説明できるスケール）の方が明らかに良い当てはまりである（例: マイナーペンタトニック+自然2度という入力に対し、2度を経過音として残す`Minor Pentatonic`ではなく、その音も含めて丸ごと説明できる`Dorian`が土台として選ばれるべき）。経過音にも欠落と同じ重みでペナルティを課すことで、この逆転を正しく解消できる。

        この3項スコアでも「完全一致するスケールが常に唯一の最高得点になる」性質は保たれる: 入力と完全に一致するスケールのスコアは常にそのスケール自身の音数と等しくなり、（a）それより音数の多い上位互換スケールは欠落分のペナルティで必ず下回り、（b）それより音数の少ないサブセットのスケールは経過音のペナルティで必ず下回る。よって大きいスケールが「なんでも飲み込んで」安易に勝つことも、小さいスケールが説明しきれない音を無視して安易に勝つこともない。
    *   **経過音 (Passing Notes) の表記:** 
        採用されたBase Scaleの構成音に含まれない度数が入力に存在する場合、それらは経過音や装飾音として扱い、スケール名の末尾に `(+b5, M7)` のようにカッコ付きで併記する（完全一致の場合はこの括弧は付けない）。また経過音と認定されたNoteは、指板図上で`ghost: true`を指定した場合と同様の表示（点線・半透明）を行う。
    *   **フォーマット:** ルート名/`□` と スケール名 の間には半角スペースを1つ入れる。
    *   **`scaleAnalyze` (Boolean, デフォルト`false`, Local限定):** `namingMode: scale`のときのみ意味を持つ。`true`の場合、タイトルを単一のBest Match表記ではなく、上記スコアリングによる**上位5件**のランキングを複数行（1件1行、`1. `〜`5. `の連番プレフィックスを付与）で表示する。フレーズ分析で「1位以外の候補も見比べたい」用途を想定した診断的な表示のため、System/Global設定は持たず、個々の図でオンにするLocal限定のオプションとする（Vault全体のデフォルトにすると、通常のスケール図まで軒並み5行の縦長タイトルになってしまうため）。
*   スケール定義表 (`SCALES`):
    Rootはユーザーが自由に指定できる設計のため、同じ音の集まりでも起点（どの音を1として読むか）を変えると異なる度数集合になる。そのため、下記の各スケール・ファミリーは伝統的に名前が付いているものだけでなく**全ての展開形（モード）**まで網羅したテーブル駆動の配列として実装すること。同点タイブレークにも使われるため、配列内の並び順は各ファミリー内で「一般的によく使われるスケール（親スケール自身や、Phrygian Dominant・Lydian Dominantのような特に有名な派生モード）を先に、マイナーな派生モードを後に」という原則で並べること。
    *   西洋音楽の教会旋法系（メジャースケールの全7モード）、メロディックマイナー系（全7モード）、ハーモニックマイナー系（全7モード）、ハーモニックメジャー系（全7モード）、ダブルハーモニック（ビザンチン/アラビアンスケール）系（全7モード）、ナポリタンメジャー系（全7モード）
    *   ペンタトニック系（メジャーペンタトニックとその全5展開）、ドミナントペンタトニック系（全5展開）、和音階（平調子/Hirajoshi系・琉球/Ryukyu系。陰音階は平調子系の1展開形と度数集合が完全に一致するため、別エントリではなくその行の別名として統合すること。それぞれ全5展開）
    *   対称音階（ホールトーンは実質1展開のみ、オーギュメントは実質2展開のみ、ディミニッシュのWhole-Half/Half-Wholeは各2展開）、ビバップ・ドミナント/メジャーの全8展開
    *   その他（Blues, Major Blues 等、単独のヘクサトニック）
    
    派生モードの厳密な度数集合は手計算で書き下さず、親スケールの半音集合から機械的にローテーションして導出・検証すること（度数集合の重複がないかも含め、テーブル駆動のテストで検証する）。派生モードの俗称・別名は、複数の情報源で一貫して確認できる確度の高いもの（例: Phrygian Dominant, Altered/Super Locrian）のみ採用し、確信の持てないものは`(Family Mode N)`という機械的な連番表記に留めること。

#### 4.5 Typography (タイトルのSVGタイポグラフィ)

**このタイポグラフィ処理は、自動生成された`title`にのみ適用すること。** ユーザーが`title:`で明示的に指定した文字列は任意のテキスト（コード名とは限らない）であり、これを解析してしまうと、たまたま含まれる`b`や数字（例: `title: berklee`の`b`、`title: "stringSpacingAdjust: -5"`の数字）を変化記号やテンションと誤認して不自然にレンダリングしてしまう。ユーザー指定の`title`は常にプレーンテキストとしてそのまま描画すること。

自動生成タイトルの文字列だけを解析し、SVGの`<text>`要素内で`<tspan>`を用いて以下の3階層に分解して描画する:

1.  **通常サイズ (Normal):**
    *   Root音のアルファベット（`C`, `D`, `□` など）
    *   基本性質の文字列（`m`, `-`, `maj`, `sus`, `add`, `aug`, `dim`, `Δ` など）
    *   分数コードのベース音全体（`/E`, `/III` など）。自身の変化記号を含め常に通常サイズ。
    *   Omit Notationの英単語部分（`omit1`/`omit3`/`omit5`の`omit`と、それを囲むカッコ）。詳細は本節末尾の補足参照。
2.  **縮小 (Reduced / 同じBaselineのままfont-sizeを75%〜100%程度に落とす。実際に`baseline-shift`で本当に下げると不自然に見えることが分かったため、Baselineは動かさずサイズだけ落とすこと。文字が元々小さく見える場合は100%寄りでよい):**
    *   そのコード自体を定義する基本の数字。具体的には: パワーコードの`5`（`C5`）、`m7`/`maj7`/`dim7`/`ø7`/`°7`/`6`/`6/9`に現れる基本の`7`や`6`、ドミナント7thでテンションが`7`を置き換えて出来た`9`/`11`/`13`（`C9`, `C11`, `C13`）、`add9`/`add11`の数字。
    *   Omit Notationの`omit1`/`omit3`/`omit5`に現れる末尾の数字（`1`, `3`, `5`）。
3.  **上付き (Raised / Superscript, `<tspan baseline-shift="super" font-size="75%">`):**
    *   Root音自身の変化記号（例: `C#`の`#`、`Eb`の`b`）
    *   既存の7th系クオリティ（`maj7`/`m7`/ドミナント`7`）に**追加**されたテンション/オルタード表記（4.6節参照。カッコ込みで丸ごと上付き。例: `Cmaj7(9)`の`(9)`、`Cm7(9, b5)`の`(9, b5)`、jazzでカッコを付けない場合の`CΔ79`の`79`）
    *   3度・sus代替のいずれも無い`5`に**追加**された`(add6)`（4.6節2.参照。カッコ込みで丸ごと上付き。例: `C5(add6)`の`(add6)`）。上記と同じ理屈で、既存の基本表記（この場合`5`）に対する追加の装飾であるため上付きとする。
    *   ディミニッシュ・ハーフディミニッシュ・オーギュメント等の特殊記号（`ø`, `°`, `+`）

**判定の目安:** 「そのコードの種類そのものを表す数字」（Reducedの例を参照）は縮小、「既存のクオリティに追加された装飾」（カッコの中身、または`b5`単体の装飾）は上付き、という区別になる。同じ数字（例えば`9`）でも、`C9`（ドミナント7thのテンションが`7`を置き換えた場合）ではReduced、`Cmaj7(9)`（`maj7`はそのまま残しテンションを追加した場合）では上付き、と役割によって描き分けが変わる点に注意。この判定はコード名の文字列だけからは機械的に判別できないため、実装上はコード名を組み立てるロジック（4.6節）側で「どの数字がどちらの役割か」を認識できる形（例えばマーキング）でタイトル文字列を生成し、SVG描画側はそれに従って分解するとよい。

**Omit Notationの扱い（補足）:** `(omit1)` / `(omit3)` / `(omit5)`（4.6節5.参照）は、3.の「既存のクオリティに追加されたテンション/オルタード表記」とは性質が異なる——コードの構成音そのものについての注記であり、既存のクオリティの上に足された装飾ではないため、上付きにはしない。カッコ・`omit`の文字部分は1.の通常サイズ、末尾の数字（`1`/`3`/`5`）は2.の縮小表記として扱う（`maj7`の`7`と同じ扱い）。マーキングの実装上は、他のRAISEDな装飾と混同しないよう、Omitマーカーには特別なマーカーを付けず素のテキストとして生成し、上記1./2.の文字種別ルール（英字は通常サイズ、数字は縮小）にそのまま従わせるとよい。

#### 4.6 Advanced Chord Inference (テンション・sus・分数コードの判定規則)
4.4節の `Naming Mode: chord` におけるコード名自動生成ロジックを拡張し、構成音（度数）から正確なテンション表記やサフィックスを推論してください。ユーザーによって Root が確定している状態から、以下の理論ルールに従ってコード名を決定すること。

**【コード名判定の基本ルール】**
1.  **分数コード (Slash Chords) の判定と表記:**
    実際に描画される最も低いピッチの音（最も太い弦の押弦位置/開放弦。`virtual: true`のノートは常に除外。4.7節参照）が Root 音でない場合、推論されたコード名の末尾に `/` とベース音を付与すること。ただし、下記「転回形の表示制御」の設定に従う。
    *   **Absolute Mode (絶対座標):** ベース音の絶対音名（アルファベット）を付与する。（例: `Cmaj7/E`）
    *   **Relative Mode (相対座標):** ベース音の絶対ピッチが確定しないため、Rootを「I」とした**ローマ数字の度数表記（I, bII, II, bIII, III, IV, bV, V, bVI, VI, bVII, VII）**を用いて付与する。（例: `□maj7/III`, `□m7/bVII`）
    *   **転回形の表示制御 (`Show Inversions`, デフォルトoff):** ベース音の度数が、そのコード自体を構成する基本の音（3度・m3度・5度・m7度・M7度のいずれか）と一致する場合、それは単なる「転回形」（同じコードを異なる音がベースに来る形で弾いているだけ）であり、新たな和声情報を追加しない。実際のコード譜では転回形は明示しないことが非常に多いため、`Show Inversions`設定が**デフォルトでoff**の間は、この条件に一致するベース音のスラッシュ表記を省略すること（コード名自体は変えず、単に`/ベース音`部分だけを付けない）。ベース音の度数が上記のいずれにも一致しない場合（テンション音や、コード構成音に含まれない音がベースに来ている場合）は、`Show Inversions`の設定に関わらず**常に**スラッシュ表記を付与する（コード記号だけでは伝わらない情報のため）。`Show Inversions`はSystem/Global/Local（`showInversions`）で設定可能。
    *   **ベースがテンション音の場合、分子（コード記号本体）側の判定からの除外:** ベース音の度数が上記「転回形」（3度・m3度・5度・m7度・M7度）のいずれにも一致しない場合——つまりベースがテンション音、またはコード構成音に含まれない音である場合——そのベース音の度数は、3.のテンション判定・畳み込み判定に使う度数集合から除外すること。ただし、ベース**以外**の音（別の弦の別のノート）がその度数を独立して鳴らしている場合は除外しない。理由: ベースのテンションはすでに`/ベース音`表記で伝わっており、コード記号本体に同じテンションを重複して表記すると冗長で、かつ本来鳴っていないはずの上部構造があるかのように誤読される。
        *   例: Root=E、構成音が Root・m3・5度・m7度・9度(F#) で、ベースが11度(A、他の弦に同じ音なし) → ベースの11度は除外され `Em9/A`（`Em7(11)/A`ではない）。
        *   例: 上と同じだが11度(A)が別の弦にも独立して鳴っている場合 → 除外せず `Em7(11)/A`。
        *   例: Root=E、構成音が Root・m3・m7度・9度(F#)（5度なし）で、11度(A)がベースかつ別の弦にも独立して鳴っている場合 → `Em11/A`（3.の畳み込み規則とも連動する）。
2.  **Susコード (sus2 / sus4):**
    *   3度（M3, m3）が含まれず、4度(P4)が含まれる場合は `sus4` とする。
    *   3度が含まれず、2度(M2)が含まれる場合は `sus2` とする。
    *   3度・2度・4度がいずれも含まれない（Rootと5度のみ）場合は、パワーコードとして `5`（例: `C5`）とする。
    *   これら（sus4/sus2/パワーコード）に加えてb5やb13（後述）が構成音に含まれる場合も、それらを黙って無視せず後述のルールに従って独立した装飾として付与すること（例: `Csus4(b13)`）。
    *   **3度・sus代替（2度/4度）のいずれも含まれない状態で、さらに7th（m7/M7）が含まれる場合**、`Omit Notation`の設定に関わらず7thは黙って捨てずに常に表示する（例: `Root, P5, m7` → `C7`。3度が無いからといって`C5`に落として7thの情報を失わないこと）。詳細は6.（Omit Notation）参照。
    *   **sus4 + 7thの場合、9度/13度の折りたたみを適用する（このsus4自身の`4`度は3.の畳み込み規則が扱う「11度」とは別枠の、susクオリティ自体を定義する度数であり、3.の「その下の階層すべてが必要」という条件の対象外）。** sus4の`4`度と自然9度（`2`度）は別の枠（sus2の`2`度とは違い、sus4の`4`は9度と異なる音）なので両立できる。7度がドミナント（m7）で、かつ9度(M2)が含まれる場合は`9sus4`、さらに6度(M6)も含まれる場合は`13sus4`とする。**13度への畳み込みには9度が必須**——3.の畳み込みルールと同じ考え方で、9度が無い状態で6度（13度）だけが加わっている場合は`13sus4`に畳み込まず、`7sus4`を残したまま独立したカッコ付きの装飾として付与する（例: `Root, 4度, 5度, m7, 2度` → `9sus4`。`Root, 4度, 5度, m7, 6度`（9度なし）→ `7sus4(13)`）。Major7th（M7）の場合は、実用上の誤読を防ぐためテンションの折りたたみは行わず、常に`maj7sus4`とする。
    *   **sus2 + 7thの場合は、テンションの折りたたみは行わない**（sus2の`2`度自体が9度と同じ音のため、別途「9」を重ねる余地が無い）。7thは黙って捨てず`7sus2`（ドミナント）または`maj7sus2`（Major7th）とそのまま連結する。
    *   **7thが無く、6度(M6)が含まれる場合も、4.の6th同様に黙って落とさず表示する。** sus4 + 6度は`6sus4`、sus2 + 6度は`6sus2`とする（例: Root, 2度, 5度, 6度 → `□6sus2`）。sus代替（2度/4度）も3度も無い状態（パワーコードの形）で6度が加わる場合は、`Omit Notation`の設定によって表記が変わる: offのときは`5`という表記はそのまま維持し、6度は独立した付加装飾として`5(add6)`のようにカッコ付きで末尾に追加する（3.のAdd/テンションの区別とは異なり、この`(add6)`は常にカッコ付き。3度自体が無い「`5`」という基本形に対する例外的な追加のため、3度が確定している通常の`add9`/`add11`とは違い、そのまま連結すると`5add6`となり読みにくいため）。onのときは、`(omit3)`が既に「3度の役割が埋まっていない」ことを示すため、代わりに6度を主表記に昇格させて`6(omit3)`とする（5.のOmit Notationルールとの整合）。
3.  **テンションと Add の区別 (7thの有無と省略ルール):**
    *   **7度が含まれない場合 (Add):** 拡張音は付加音として扱い、`add` を使用する（例: Root, M3, P5, M2 → `Cadd9`）。3度が存在し、9度が無く4度のみが追加される場合は `add11` とする。**9度と11度の両方が追加される場合は `add9` を主表記とし、11度は独立したカッコ付きの装飾として末尾に追加する**（`add9`と`add11`をそのまま連結して`add9add11`のような表記にはしないこと。例: Root, M3, P5, M2, P4 → `Cadd9(11)`）。
    *   **7度（M7, m7）が含まれる場合 (Tension):** 拡張音はテンションとして扱う。基本となる7th系のクオリティ表記の**組み立て方は、そのクオリティが何であるかによって異なる**:
        *   **【重要】畳み込み（9度/11度/13度への `7` の置き換え）の共通ルール:** ドミナントセブンス・Major7th・Minor7th・m(maj7)（マイナー・メジャーセブンス、例: メロディックマイナーのトニック）のいずれも、畳み込みは**一段階下だけでなく、その下の階層の度数がすべて構成音に含まれている場合にのみ**行う。9度への畳み込みは9度単体で成立する。11度への畳み込みには9度・11度の**両方**が必要。13度への畳み込みには9度・11度・13度の**すべて**が必要。11度は長3度に対するavoid noteとして意図的に省略されることが非常に多く、その省略はコードの実質的な性格を変えるため、「9度さえあれば11度なしで13度に畳み込んでよい」という簡略ルールは**採用しない**（旧仕様からの変更点）。
            *   畳み込みの条件を満たさない、より上位の度数（11度・13度）が独立して構成音に含まれる場合は、まとめずに黙って無視せず、既存のベース表記（`7`, `maj7`, `m7`, `m(maj7)`）を残したままカッコ付きの独立した装飾として追加する。複数該当する場合はカンマ区切りで1つのカッコにまとめる（例: 9度なしで11度のみ → `C7(11)`。9度なしで11度・13度の両方 → `C7(11, 13)`。9度はあるが11度が無く13度がある → `C9(13)`）。
        *   **ドミナントセブンス (Root, M3, P5, m7) の場合:** 畳み込みが成立する場合、その中で最も高いテンションで `7` を上書きし `C9`, `C11`, `C13` と表記する（例: 7度+9度+11度 → `C11`。7度+9度+11度+13度 → `C13`）。
        *   **Major7th (`maj7`/`Δ7`) / Minor7th (`m7`/`-7`) / m(maj7) の場合:** 畳み込みが成立する場合、ベースとなる四和音表記（`maj7`/`m7`/`m(maj7)`）のテンション数字部分を置き換えて `Cmaj9`, `Cm9`, `Cm(maj9)` のように表記する（`Chord Symbol Style`が`jazz`の場合はそれぞれ `CΔ9`, `C-9`, `C-Δ9`）。畳み込みが成立しない場合は上記の共通ルール通り、ベース表記を残したままカッコ付きで付与する（例: `Cmaj7(11)`, `Cm7(11)`, `Cmaj9(13)`, `Cm(maj7)(11)` / jazzではカッコを付けずそのまま連結、例: `CΔ7` + `(9)` → `CΔ79`）。
    *   **オルタードテンション（b9 / #9 / #11 / b13 / b5）の扱い:** 3.のテンション付きコードに限らず、2.のsus/パワーコードを含むすべてのケースで、以下のオルタードテンションが構成音に含まれる場合は、それらを独立した装飾として末尾に追加する（黙って無視しないこと）。`Chord Symbol Style`が`standard`/`berklee`ならカッコ付き・カンマ区切り（例: `C9(b5)`, `Cm7(9, b5)`, `Csus4(b13)`, `G7(#9, b13)`）、`jazz`ならカッコなしでそのまま連結する（例: `C9b5`, `Csus4b13`）。
        *   **b9:** b2度が構成音に含まれる場合。他の度数と異名同音の衝突が無いため、常に♭9として扱う。
        *   **#9:** m3度が構成音に含まれ、かつ**M3度（長3度）も同時に含まれる場合に限り**、そのm3を#9として扱う。M3度が無い場合のm3は、通常通りそのコードの短3度（マイナーコードのクオリティ）として扱われ、#9ではない。
        *   **#11:** b5度が構成音に含まれ、かつ**5度(P5)も同時に含まれる場合に限り**、そのb5を#11として扱う。
        *   **b13:** m6度が構成音に含まれ、かつ**5度(P5)も同時に含まれる場合、または7th（m7/M7）が同時に含まれる場合**、そのm6を♭13として扱う。ギターでは7thコードのボイシングにおいて、オルタードテンションを保持したまま押弦の都合上5度(P5)を省略する（ミュートする）ことが非常に多いため、5度(P5)の有無だけでなく7thの有無も判定材料に含めること。5度(P5)・7thのいずれも含まれない状態でのm6は増5度（augmented。基本的なトライアド・クオリティを定義する場合のみ）の一部として解釈されるため、この♭13ルールの対象外。
        *   **b5:** 5度(P5)が含まれない状態でのb5は、上記#11ルールの対象外で、コード自体の減5度（flat 5th）として扱う（従来通り）。
        *   （4.2節の度数表参照。1オクターブ内の度数体系では各オルタードテンションと別の度数が同じ度数ラベルで表現されるため、いずれも「対になる自然な度数が同時に含まれるかどうか」で意味を判定する。b9のみ、対になる自然度数との異名同音衝突が無いため常にオルタードとして扱う。）
4.  **6th と 6/9 コード:**
    *   7度が無く、6度(M6)が含まれる場合は `6`（例: `C6`, `Cm6`）とする。
    *   上記に加えて9度(M2)も含まれる場合は `6/9` とする。
    *   このケースは3.のテンション/カッコの対象外（常にカッコなし）。
    *   **上記に加えて11度(P4)も含まれる場合**、11度は`6`/`6/9`という主表記には畳み込まず、独立したカッコ付きの装飾として末尾に追加する（例: `C6(11)`, `C6/9(11)`）。このカッコは`Chord Symbol Style`が`jazz`であっても省略しない（`6`/`6/9`という主表記自体が3.のテンション畳み込みルールの対象外であるため、jazzのカッコ省略ルールもここには及ばない）。
5.  **Omit Notation (`omitNotation`, デフォルトoff):** `Omit Notation`がonの場合のみ、理論上期待される構成音が実際には含まれていないことを、末尾に丸括弧書きで明示する。**タイポグラフィは上付き（Superscript）にはしない**——既存のクオリティに追加されたテンション/オルタード表記（4.5節3.）とは異なり、Omitマーカーはコードの構成音そのものについての注記であり、既存表記への「追加の装飾」ではないため。`maj7`等と同じく、英字部分は通常サイズ・末尾の数字（`1`/`3`/`5`）だけ縮小表示とする（4.5節の判定ルール・補足を参照）。offの場合はこの5.のルールを一切適用しない（4.6節の他のルールはOmit Notationの設定に関わらず常に適用される）。
    *   **`(omit3)`:** 2.のsus代替（2度/4度）が無く、かつ3度（M3, m3）も含まれない場合。7thが含まれる場合は7th表記（`7`, `maj7`等）を残したまま末尾に`(omit3)`を追加する（例: `G7(omit3)`）。7thも含まれない純粋なパワーコードの場合は、`5`という表記自体を`(omit3)`に置き換える（`(omit3)`という表記自体がすでに「Root+5度のみ」であることを示すため。例: `C5`ではなく`C(omit3)`）。6度が含まれる場合はこの例外にさらに例外が付く: `Omit Notation`がonのときは`5(add6)`（2.末尾参照）ではなく`6(omit3)`とする（`5`を`(omit3)`に置き換える場合と同じ理屈で、`6`という主表記自体がすでに「3度が埋まっていない」ことを示せるため、offのときの`(add6)`という追加カッコ表記は不要になる）。
    *   **`(omit5)`:** 5度・b5度・m6度のいずれも構成音に一切含まれない場合（オルタード5thも含め、5度に相当する音が本当に何も無い場合のみ。b5やb13/#11としてオルタードされている場合は「5度が省略されている」わけではないので対象外）。
    *   **`(omit1)`:** Rootの絶対ピッチ自体は確定している（すなわちどこかに`label: root`が指定されている）が、実際に押弦・発音されている音（4.7節の`virtual`を除く）の中にRootの音が1つも無い場合。仮想Root（4.7節参照）を使う場合に主に該当する。
    *   複数該当する場合は、他のテンション同様カンマ区切りで1つの括弧にまとめる（例: `G7(#9, b13, omit5)`）。`(omit1)`はスラッシュ表記（分数コード）よりも前に置く（例: `G7(#9, b13)(omit1)/B`。スラッシュのベース音は常にコード表記全体の最後尾に来るという慣習を優先する）。
    *   これらのOmit系装飾は、`Chord Symbol Style`が`jazz`であっても常にカッコ付き・カンマ区切りで表記する（他のテンション/b5の装飾とは異なりカッコを省略しない）。`omit3`等は`b13`や`#9`のような短い記号ではなく英単語であるため、jazz形式の「カッコなしでそのまま連結」を適用すると`C9b5omit5`のように読みにくくなるため。
6.  **表記の流儀 (Chord Symbol Style):**
    最終的な文字列の組み立ては、必ず `Chord Symbol Style` (`standard`, `berklee`, `jazz`) のルールに従って記号を出し分けること。
    *   `standard`: `m7(b5)`, `dim7`, `maj7`, `m`。3.で追加されるテンション/b5はカッコ付き。
    *   `berklee`: `-7(b5)`, `dim7`, `maj7`, `-`。3.で追加されるテンション/b5はカッコ付き。複数該当する場合は `(9, b5)` のようにカンマで区切る。
    *   `jazz`: `ø7`, `°7`, `Δ7`, `-`。3.で追加されるテンション/b5にカッコは付けず、そのまま連結する（5.のOmit系装飾を除く）。

#### 4.7 Virtual Notes (仮想ノート・実際には鳴らない基準ピッチ)

**動機:** `G7(#9, b13)`のようなオルタード・ドミナントコードは、運指の都合上Rootを省略して弾かれることが非常に多い（テンションノートが色彩を担い、Rootはベース奏者や他の楽器に任せる等）。しかし本プラグインの度数・コード名の自動算出は、`label: root`が指し示す実在の（`s`, `f`）位置のピッチを基準にしか行えないため、Root自体を`notes`から完全に省いてしまうと算出の基準点を失ってしまう。`virtual: true`はこの問題を解決する：「実際には押弦・発音しないが、基準ピッチとしては存在する」という音を表現できるようにする。

**仕様:**
*   ノートの`virtual`フィールド（3.1節参照）がtrueの場合、そのノートは以下のように扱われる:
    1.  **度数計算・コード名算出への寄与:** 通常のノートと全く同じように、そのピッチが度数計算に使われ、`presentDegrees`（4.2節・4.6節の判定材料となる度数集合）にそのまま加算される。`label: root`と`virtual: true`を組み合わせることで「仮想Root」となり、そこを基準に他のノートの度数が計算される。
    2.  **描画:** 図形（丸・四角・三角等）を一切描画しない。代わりに、そのノートの算出済みラベル文字列（`Label Mode`が`interval`ならR/度数、`note`なら音名、カスタム`label`があればそれ）を、丸括弧で囲んだテキストのみとして、通常のノートと同じフレット位置に描画する（例: Rootなら`(R)`、5度の仮想ノートなら`(5)`）。ラベルが空文字列の場合は何も描画しない。指番号（`finger`）も描画しない。文字色は通常のノートより控えめな色（`--text-muted`相当）にすること。
    3.  **分数コード（4.6節1.）のベース音判定から除外:** 「実際に描画される最も低いピッチの音」を求める際、`virtual: true`のノートは物理的に鳴っていないものとして常に除外する。
    4.  **`(omit1)`との連携:** Rootの絶対ピッチ自体は`label: root`により確定しているが、実際に押弦・発音されているノート（`virtual`ではないノート）の中にRootのピッチを持つものが1つも無い場合、`Omit Notation`がonなら自動生成タイトルに`(omit1)`が付与される（4.6節5.参照）。仮想Rootを使うケースでよく該当するが、判定条件はあくまで「物理的にRootの音が鳴っているか」であり、`virtual`フィールドの有無そのものではない（例えば別の弦でRootが実際に押弦されていれば、仮想Rootを併用していても`(omit1)`は付与されない）。
*   `virtual`はRoot以外の任意の音にも指定できる（例: 「仮想5度」として押弦を省略しつつ度数計算だけに関与させる）。
*   グリッド上の位置（弦・フレット数の自動拡張、`visible`範囲など）には通常のノートと同様に関与する（見た目の描画位置自体は確保される）。

#### 4.8 Degree Name Root Notation (keyを基準としたローマ数字表記)

**動機:** ダイアトニック・コードの機能分析（例: キーCにおけるBm7(b5)は「VIIm7(b5)」＝vii°）や、モーダルなスケール分析（「VII Locrian」）を行いたい場合、実音名ではなくキーからの相対的な度数（ローマ数字）でルートを表記したい需要がある。これは4.6節1.の「Relative Modeにおけるスラッシュ・ベースのローマ数字表記」（あくまで**その図自身のRoot音**からの相対度数で、絶対ピッチが未確定なムーバブルな形専用）とは目的も基準点も異なる別の仕組みであり、こちらは**ユーザーが明示的に指定した`key`**を基準点とし、Absolute Modeの図でも機能する。

**仕様:**
1.  **`key` (Local限定, 3.2節参照):** トニックの音名文字列（例: `"C"`, `"F#"`, `"Bb"`）。有効な音名として解釈できない場合はエラーとする（5節参照）。
2.  **`Root Notation` (`rootNotation`, System/Global/Local, 2.1節参照):** `absolute`（デフォルト）/ `degree`。
3.  **Degree Name表記が実際に適用される条件:** 以下の**すべて**を満たす場合にのみ、ルート音（および該当する場合はスラッシュ・ベース、下記5.参照）がローマ数字表記になる。
    *   `Root Notation`が`degree`であること。
    *   その図に`key`が指定されていること。
    *   その図がAbsolute Mode（4.1節）であること。**`key`の有無に関わらず**、フレット位置の指定が無くRoot音の絶対ピッチ自体が同定できない場合（＝Relative Mode。ムーバブルな形）は、`key`からの相対度数も原理的に計算できないため、常に従来通り`□`にフォールバックする。
    *   上記のいずれかを満たさない場合は、常に`absolute`と同じ挙動（実音名、Relative Modeなら`□`）にフォールバックする。
4.  **ローマ数字への変換:** Root音の絶対ピッチと`key`の絶対ピッチの半音差分を`%12`で正規化し、4.6節1.と同じローマ数字の度数表（I, bII, II, bIII, III, IV, bV, V, bVI, VI, bVII, VII）を用いて表記する（例: `key: C`に対しRootがB(11半音上)なら`VII`）。
5.  **分数コードのベース音との連携:** Degree Name表記が適用されている場合、分数コード（4.6節1.）のベース音も、そのコード自身のRootからの相対度数ではなく、**同じく`key`を基準としたローマ数字**で表記する（例: `key: C`でRootがB、ベースがCの場合 `VIIm7(b5)/I`）。これにより1つのタイトル内でルートとベースの基準点が食い違わない。この連携はDegree Name表記が実際に適用されている場合のみで、Relative Modeの図が使う「Root自身からの相対度数」表記（4.6節1.）はこの仕様の対象外のまま変更されない。
6.  **Naming Mode: scaleとの連携:** `namingMode: scale`（4.4節）のタイトルでも、ルート部分（通常は実音名または`□`が入る位置）に同じくローマ数字を用いる（例: `key: C`でRootがBの`Locrian`なら`VII Locrian`）。
7.  **タイポグラフィ:** ローマ数字（およびその先頭に付く`b`の変化記号）は4.5節の文字種別ルールがそのまま適用され、新たなマーカーは不要（アルファベットは通常サイズ、先頭の`b`は上付き）。
8.  **`Key`ラベルの表示:** `key`が指定されている図は、`Root Notation`の値に関わらず（`absolute`のままでも）、図の左上に`Key: C`のようなラベルを常に表示する。実音名の表記には`Accidental`（2.1節）の設定を反映する。

**例（key: C, Root Notation: degree）:**
```fretboard
key: "C"
rootNotation: degree
startFret: 0
notes:
  - {s: 2, f: 0, label: root}
  - {s: 4, f: 0}
  - {s: 1, f: 1}
  - {s: 5, f: 0}
```
上記は自動生成タイトルが `VIIm7(b5)` になり、左上に `Key: C` が表示される。

## 5. Error Handling & Deliverables
*   **Error Handling (Local):** ```fretboardブロック内のYAMLに構文エラーや不正な値があった場合、プラグインをクラッシュさせず、そのコードブロック内に赤字でエラー内容のテキストを表示して安全に中断してください。
*   **Error Handling (Global):** Vault共通の`fretboard-renderer.yaml`に構文エラーや不正な値があった場合、Vault内の描画全体をクラッシュさせず、Notice通知で一度だけエラーを表示した上でSystem設定にフォールバックしてください（2.2節参照）。
*   **未知のキーの扱い:** Local・Global（`notes`/`barre`/`boxes`の各エントリを含む）のいずれも、認識できないキー（例: `frets`のタイポである`flets`）が含まれる場合は**黙って無視せず、エラーとして扱う**こと。タイポを黙って無視すると「設定したのに何も起きない」という診断が非常に困難な不具合になるため、必ず気づけるようにする。
*   **Deliverables:** TypeScript (`main.ts` 及び適宜分割したファイル) を生成してください。SVGのスタイリングには Obsidian の CSS 変数 (例: `var(--text-normal)`, `var(--background-modifier-border)`) を使用し、ライト/ダークテーマに自動追従させてください。
